public String parseInput(String input) {
    String[] parts = input.split(",");
    int value = Integer.parseInt(parts[0]);
    String name = parts[1];
    
    if (value > 100) {
        return "Large: " + name;
    } else {
        return "Small: " + name;
    }
}